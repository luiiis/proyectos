services:
  backend:
    build: { context: ./backend, dockerfile: Dockerfile }
    container_name: mini16-backend
    ports:
      - "8080:8080"
    volumes:
      - uploads:/app/uploads

volumes:
  uploads:
